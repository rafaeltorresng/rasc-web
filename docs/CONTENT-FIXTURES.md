# Marketing Content Fixtures

Use only fictional, synthetic fixture text. Do not use real personal, customer, company, confidential, or maintainer note content. The name Maya and the other examples below are part of the approved synthetic landing fixture.

## Hero / Refine

Exact candidate input:

    ask maya if launch copy still works and send updated screenshots

Approved target characteristics:
- preserve Maya, launch copy, and updated screenshots;
- keep the meaning and English language;
- improve capitalization/grammar conservatively;
- add no facts, deadlines, or commitments;
- do not force a changed result: a real noChange is part of Rasc’s behavior.

Approved example outputs in docs/ASSET-PLAN.md:
- Ask Maya if the launch copy still works and send the updated screenshots.
- Ask Maya whether the launch copy still works and send the updated screenshots.
- Ask Maya if the launch copy still works, then send the updated screenshots.

Any output used in the landing must be generated by the real app during the recorded capture build. Do not edit the result in post. The 2026-09-23 five-run validation is recorded in docs/HERO-REFINE-VALIDATION.md and is not stable enough to approve this input/output pair for a definitive demo yet.

## Home notes

Create these nine fictional notes, preserving their deliberately mixed voice.

### Kept

1. Note text (first line is the app-derived title):

       Launch page
       Ask Maya if the launch copy still works and send the updated screenshots.
2. Note text:

       Book flights
       Compare the morning flights before prices move again.
3. Note text:

       API retries
       Keep retries bounded. We should fail visibly instead of hiding a persistent upstream problem.

### Unfinished

4. Note text:

       pricing idea
       one-time purchase maybe better fit than subscription because inference cost basically zero
5. Note text:

       follow up
       send the revised proposal after lunch
6. Note text:

       onboarding thought
       maybe shortcut practice should happen before explaining refine
7. Note text:

       weekend
       look up that restaurant João mentioned
8. Note text:

       error handling
       distinguish user cancellation from actual provider failure
9. Note text:

       article
       interesting idea: tools that disappear after the job is done

The app has no separate title field: its displayed title is derived from the first meaningful content line. Include the title line followed by the note body as shown. Keep the capitalization, fragments, and telegraphic quality. Do not polish all notes into marketing copy. The example says “Today / unfinished” and “Earlier” to suggest natural temporal grouping. Creating notes through the app in one session gives them current timestamps; they will naturally appear together under the current day. Do not falsify note dates for a screenshot. If separate day groups are required, record that as an explicitly approved fixture-seeding need before proceeding.

## Search

- Query: launch
- Expected visible result: Launch page, with the matching launch-copy text.
- Capture the real in-scratchpad Search UI; do not recreate it in web markup.

## Capture environment

The production app uses:

    ~/Library/Application Support/Rasc/Rasc.sqlite

There is currently no database-path override or marketing fixture mode. Use a separate local macOS user account, populate these fixtures there, and do not seed, copy over, or overwrite the maintainer’s normal Rasc database. The separate account keeps its own app data. Create and Keep the three designated notes in that account; leave the other six unfinished.
